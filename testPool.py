from multiprocessing import Pool

def f(i):
    print(i,end=' ')

def main():
      # ????? 3 ???????
    pool = Pool(processes=3)
    for i in range(30):
           # ?? apply ????????????????? f???? i
        pool.apply(f, (i,))

if __name__ == '__main__':
    main()
